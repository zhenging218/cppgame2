#include "engine.hpp"
#include "gg_integration.hpp"

namespace cppengine {

    GladModelContext::GladModelContext() {

    }

    ObjectHandle<ModelHandle> GladModelContext::getModel(ModelID id) const {

    }

    ModelID GladModelContext::loadModel(std::string const &name, Vector3 const *vertices, std::size_t const *indices, std::size_t vertexCount, std::size_t indexCount) {

    }

    GladModelContext::~GladModelContext() {

    }
}
